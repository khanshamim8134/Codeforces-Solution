#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const ll limit = 1000000;
vector<ll> nod(limit + 1, 0);


int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    ll i, j,mexi=-1;

    // calculate number of divisors
    for (i = 1; i <= limit; i++) {
        for (j = i; j <= limit; j += i) {
            nod[j]++;
           // mexi = max(mexi,nod[j]);
        }
    }

    // check array for products of two distinct primes up to 250
    bool check[251]={false};
    int arr[] = {
        6,10,14,15,21,22,26,33,34,35,38,39,46,51,55,57,58,62,65,69,
        74,77,82,85,86,87,91,93,94,95,102,106,111,115,118,119,122,123,129,133,134,141,
        142,143,145,146,155,158,159,161,166,177,178,183,185,187,194,201,202,203,205,206,209,213,214,
        215,217,218,219,221,226,235,237
    };
    int n = sizeof(arr)/sizeof(arr[0]);
    for(i=0;i<n;i++){
        if(arr[i]<=250)
            check[arr[i]] = true;
    }
      ll cnt=0;
    // collect numbers whose divisor count matches
    for (i = 1; i <= limit; i++) {
        ll k = nod[i];
        if(check[k]) {
            cnt++;
            if(cnt%9==0){
            cout<<i<<endl;}
        }
    }
    //cout<<mexi<<endl;

    return 0;
}
